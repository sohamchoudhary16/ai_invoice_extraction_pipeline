# ── Core PDF & OCR ───────────────────────────
pymupdf==1.24.5
pytesseract==0.3.13
Pillow==10.4.0
pypdf==4.3.1

# ── Image preprocessing ───────────────────────
opencv-python-headless==4.10.0.84    # headless = no GUI dep, fine for servers

# ── LangChain (kept for PromptTemplate only) ─
langchain-core==0.2.38
langchain-ollama==0.1.3

# ── HTTP client for Ollama (direct, no LangChain overhead) ──
requests==2.32.3

# ── XML parsing (ZUGFeRD) ────────────────────
lxml==5.2.2

# ── Data handling ────────────────────────────
pandas==2.2.2
numpy==1.26.4

# ── Config & env ─────────────────────────────
python-dotenv==1.0.1
pyyaml==6.0.1

# ── Language detection ────────────────────────
langdetect==1.0.9

# ── Utilities ────────────────────────────────
tqdm==4.66.4
